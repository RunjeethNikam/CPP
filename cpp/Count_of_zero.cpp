#include<bits/stdc++.h>
#define lli long long int
using namespace std;
int BS(vector<lli> given, lli lo,lli hi){
    while(lo <= hi){
        
    }
}
int main(int argc, char const *argv[])
{
    lli testCases;
    cin>>testCases;
    while(testCases--){
        lli size;
        vector<lli> given(size);
        for (lli i = 0; i < size; i++)
        {
            cin>>given[i];
        }
        
    }
    return 0;
}
