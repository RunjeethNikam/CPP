#include<bits/stdc++.h>
using namespace std;
int kn(int mxCapacity, int numberOfItems, vector<int> wts){
    
}
int main(int argc, char const *argv[])
{
    int t;
    cin>>t;
    while (t--)
    {
        int k, n;
        cin>>n>>k;
        vector<int> packages(k);
        for(int i = 0;i<k;i++){
            cin>>packages[i];
        }
        cout<<kn(k, k, given);
    }
    
    return 0;
}
